'use client'

import React from 'react';
import GuestComponent from '@/app/components/moduloGuest';

const Guest: React.FC = () => {
  return (
  
      <GuestComponent />

  );
};

export default Guest;
